/*import { MatSnackBar } from "@angular/material/snack-bar";
import { ApiBaseError } from "../classes/api/api-base-error";

export function apiErrorHandler(snackBar: MatSnackBar, error: ApiBaseError) {
  console.error(error);
  snackBar.open(error.errors[0].errorMessage, undefined, { duration: 5000 });
}*/
